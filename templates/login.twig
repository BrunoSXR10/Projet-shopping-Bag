<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}}</title>
</head>
<body>

    <h1>{{ title }}</h1>
    <a href="#">{{ link }}</a> 
    
    <ul>
        <p>{{ listTitle }}</p>
        {% for item in items %}
        <li>
          <a href="#">{{ item }}</a>
        </li>
        {% endfor %}
      </ul>
      <a href="/login.php">{{ conecter }}</a>

    <h3>{{ heading }}</h3>
    <p>{{ subtitle }}</p>
    <form>
        <div>
            <label for="">{{user}}</label>
            <input type="text">
        </div>
        <div>
            <label for="">{{motpasse}}</label>
            <input type="text">
        </div>
        <button>{{ aaaa }}</button>
    </form>
</body>
</html>