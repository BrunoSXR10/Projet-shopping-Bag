<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ name }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <a href="#">{{ payer }}</a>

    <ul>
      <p>{{ listTitle }}</p>
      {% for item in items %}
      <li>
        <a href="#">{{ item }}</a>
      </li>
      {% endfor %}
    </ul>
    <a href="./login.php">{{ account }}</a>

    <h2>{{ heading }}</h2>
    <p>{{ subtitle }}</p>
  </body>
</html>
